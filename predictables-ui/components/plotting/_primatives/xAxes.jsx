// import { xAxesProps } from '../interfaces';

const xAxes = ({ height = 500, width = 750, offset = 20, color = 'black' }) => {
  // const xAxes = ({
  //   height = 500,
  //   width = 750,
  //   offset = 20,
  //   color = 'black',
  // }: xAxesProps) => {

  return <svg></svg>;
};

export default xAxes;
